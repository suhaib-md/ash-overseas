# Dependencies
node_modules/

# Build output
dist/

# Cloudflare / Wrangler local state
.wrangler/

# Local secrets — NEVER commit (see .dev.vars.example)
.dev.vars
.env
.env.*
!.env.example

# Test / coverage
coverage/

# Local SQLite artifacts
*.sqlite
*.sqlite-*

# DB dumps (financial data — never commit; migrations/*.sql stay tracked)
/backup.sql
/restore.sql
/backups/

# OS / editor
.DS_Store
Thumbs.db
*.local

# Claude Code local settings (session-recorded permissions, not project config)
.claude/
